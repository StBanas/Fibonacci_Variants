package pbc.fibonacci;

import jdk.internal.net.http.common.Pair;
import pbc.car.Car;

public class FibonacciTuple {
   int n;
}

//
//

        // Wariant 1
        // Utworzenie tupli dwuelementowej - w głębii jest to dwuelementowa mapa; przypisanie wyrazów kolejnych
        // ciagu Fibonaccciego a następnie zmiana ich pozycji w
    Pair<Integer, Integer> pair = new Pair<Integer, Integer>(n, n-1);
        if n = 1 {

                }
                result = 1)
       else if  n = 2
                result = 2}
       else result = (n - 1) + n ;
}

    // Wariant 3
    // Wykorzystanie rozwinięcia dwumianu Newtona - wariant własny


}
